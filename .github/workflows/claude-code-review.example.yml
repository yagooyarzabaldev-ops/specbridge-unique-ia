name: Claude Code Review Example

on:
  pull_request:
    branches:
      - main

jobs:
  claude-code-review-example:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Example only
        run: echo "Claude Code review workflow is not active yet."
