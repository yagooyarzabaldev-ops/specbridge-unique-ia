name: Claude Code Execute Example

on:
  workflow_dispatch:

jobs:
  claude-code-execute-example:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Example only
        run: echo "Claude Code execution workflow is not active yet."
