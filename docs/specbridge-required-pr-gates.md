# SpecBridge Required PR Gates

SpecBridge currently protects `main` with required pull request checks.

Required gates:

- Foundation Validation
- SpecBridge Review Gate

This document exists as a controlled verification artifact proving that branch protection and PR-level validation are active.
